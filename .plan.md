# Plan: Remove h key toggle and implement arrow navigation for bookmarks tabs

## Current Issue
- User wants to remove `h` key toggle functionality in bookmarks tab
- Replace with left/right arrow navigation for tabs when sidepanel is active and tab is bookmarks

## Changes Required

### 1. Update main.rs keybindings
- Remove `h` key toggle from bookmarks tab keyhandling
- Add left/right arrow key navigation for bookmarks tabs when sidebar is visible and tab is 4 (bookmarks)
- Ensure this only affects the tab navigation, not the input field within bookmarks

### 2. Update help modal
- Update help documentation to reflect new keybindings
- Remove mention of `h` toggle for bookmark views
- Add left/right arrow navigation for bookmark tabs

## Implementation Details

### Keybinding Changes:
- **Remove**: `KeyCode::Char('h')` toggle in bookmarks tab
- **Add**: `KeyCode::Left` / `KeyCode::Right` for tab navigation when `app.sidebar_visible && app.sidebar_tab == 4`
- **Preserve**: Existing `h` functionality for input movement within bookmark input field

### Help Modal Updates:
- Change: "h: Toggle Bookmarks/Recent" to arrow navigation
- Update: Sidebar keybindings section with new navigation method

## Files to Modify
1. `src/main.rs` - Keybinding updates
2. `src/ui/mod.rs` - Help modal updates

## Success Criteria
- Left/right arrows navigate bookmark tabs when sidebar is visible
- `h` key continues to work for cursor movement in input field
- No other functionality is broken
- Help modal reflects new keybindings accurately